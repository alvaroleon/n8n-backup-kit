# Container ID or name of the n8n MAIN container
CID=e9c1c813621a

# Target container's encryption key (required for export/import)
# NEVER commit your real key to source control.
N8N_ENCRYPTION_KEY=replace-with-your-encryption-key

# Absolute shared directory path INSIDE the container (bind-mounted to host)
SHARED_DIR=/home/node/shared

# Optional: local directory on host where backups will be copied (default: current dir)
OUTPUT_DIR=.

# Optional: backup name prefix (default: n8n-backup)
BACKUP_PREFIX=n8n-backup